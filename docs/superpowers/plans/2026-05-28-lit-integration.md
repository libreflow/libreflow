# Lit Integration — Phase 0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Installer Lit comme dépendance runtime de libreflow et migrer l'implémentation interne de `toast()` / `toastWithAction()` (`ui.js`) vers un Web Component `<lf-toast-stack>`, sans modifier l'API publique ni les call sites.

**Architecture:** Lit 3.x (JS pur, sans decorators) ajouté à `dependencies`. Un nouveau dossier `frontend/src/components/` accueille le 1er composant `<lf-toast-stack>`. La logique d'état est extraite dans un module pur testable. `ui.js#toast()` et `ui.js#toastWithAction()` deviennent des façades qui délèguent à un singleton du composant, tout en préservant strictement l'API existante (signature, types, durées par type, a11y, cap à 5 toasts, click-to-dismiss, retour de fonction `remove()` avec `.update()`).

**Tech Stack:** Lit 3 (`lit` package), Vite 8 (existant), Node 20 + `core.test.cjs` (vanilla assert harness existant), Playwright (suite visuelle existante), `perf-budgets.json` + `bench.cjs` (gating perf existant).

**Spec reference:** `docs/superpowers/specs/2026-05-28-lit-integration-design.md`

**Note importante :** la spec décrivait une API toast simplifiée ; au moment d'écrire ce plan on a constaté que la `toast()` actuelle est plus riche (retour de `remove()` avec `.update()`, 5 types — `info`/`success`/`error`/`warning`/`loading` —, durées par type, a11y `role=alert`/`aria-live=assertive` pour `error`/`warning`, bouton close ×, cap à 5 toasts, progress bar). **Le plan préserve intégralement ce comportement** ; aucune régression d'API n'est acceptée.

---

## File map

| Path | Action | Owner task |
|---|---|---|
| `package.json` | modify (add `lit` to `dependencies`) | 1 |
| `package-lock.json` | regenerated by `npm i` | 1 |
| `frontend/src/components/` | new dir | 2 |
| `frontend/src/components/lf-toast-stack.logic.js` | new (reducer pur + constantes) | 2 |
| `frontend/tests/core.test.cjs` | modify (append section "lf-toast-stack reducer") | 3 |
| `frontend/src/components/lf-toast-stack.js` | new (Web Component Lit) | 4 |
| `frontend/src/ui.js` | modify (toast + toastWithAction deviennent façade) | 5 |
| `frontend/src/style.css` | modify (CSS custom properties si nécessaire) | 5 |
| `frontend/tests/visual/lit-toast.spec.js` | new (Playwright visual test) | 6 |
| `perf-budgets.json` | modify (potentiellement rééquilibré si bundle dépasse) | 7 |
| `CLAUDE.md` | modify (note Lit + dossier components/) | 8 |

---

## Task 1 : Ajouter Lit comme dépendance runtime

**Files:**
- Modify: `package.json` (add `"lit": "^3.x"` to `dependencies`)
- Modify: `package-lock.json` (auto par `npm install`)

- [ ] **Step 1 : Installer lit en dépendance runtime**

```bash
npm install --save lit
```

Expected: la console affiche `added 3 packages` ou similaire (lit + lit-html + @lit/reactive-element).

- [ ] **Step 2 : Vérifier que `lit` apparaît dans `dependencies` (pas devDependencies)**

```bash
node -e "const p = require('./package.json'); if (!p.dependencies.lit) { console.error('lit absent de dependencies'); process.exit(1); } console.log('lit version:', p.dependencies.lit);"
```

Expected: affiche `lit version: ^3.x.y`. Exit 0.

Si lit est tombé dans `devDependencies`, le déplacer manuellement vers `dependencies`.

- [ ] **Step 3 : Vérifier l'offline (pas de phone-home à l'import)**

```bash
node -e "import('lit').then(m => console.log('lit imported OK, exports:', Object.keys(m).slice(0, 5).join(', '))).catch(e => { console.error(e); process.exit(1); })"
```

Expected: affiche `lit imported OK, exports: LitElement, html, css, ...`. Pas d'erreur réseau.

- [ ] **Step 4 : Build doit toujours passer**

```bash
npm run vite:build
```

Expected: build complet sans erreur. Pas encore d'usage de Lit, donc bundle inchangé (sauf si Vite hoist lit dans node_modules — vérifier dans Task 7).

- [ ] **Step 5 : Commit**

```bash
git add package.json package-lock.json
git commit -m "feat(deps): add lit 3.x as runtime dependency (Phase 0)"
```

---

## Task 2 : Module logique pur (reducer + constantes)

**Files:**
- Create: `frontend/src/components/lf-toast-stack.logic.js`

- [ ] **Step 1 : Créer le dossier `components/`**

```bash
mkdir -p frontend/src/components
```

- [ ] **Step 2 : Écrire le module logique**

Créer `frontend/src/components/lf-toast-stack.logic.js` :

```js
// LibreFlow — lf-toast-stack.logic.js
// Module logique pur du Web Component <lf-toast-stack>.
// Aucun import Lit, aucune référence DOM — testable directement en CJS.
//
// Exports :
//   TOAST_DUR        — durées par défaut par type (ms)
//   TOAST_TYPES      — liste des types valides
//   toastReducer     — (items, action) → items'
//   normalizeType    — type|undefined → type valide (fallback 'info')
//   resolveDuration  — (typeOrItem, explicitDur?) → durée résolue (ms)

'use strict';

// Durées par défaut alignées avec l'ancien _TOAST_DUR de ui.js (préservation contrat)
export const TOAST_DUR = Object.freeze({
  info:    3000,
  success: 2600,
  error:   8000,
  warning: 6000,
  loading: 120000,
});

export const TOAST_TYPES = Object.freeze(['info', 'success', 'error', 'warning', 'loading']);

export function normalizeType(t) {
  return TOAST_TYPES.includes(t) ? t : 'info';
}

export function resolveDuration(type, explicitDur) {
  if (typeof explicitDur === 'number' && explicitDur > 0) return explicitDur;
  return TOAST_DUR[normalizeType(type)];
}

/**
 * Reducer pur de la pile de toasts.
 * @param {Array} items   pile courante (immutable, jamais mutée)
 * @param {Object} action { type, ... }
 * @returns {Array} nouvelle pile
 *
 * Actions supportées :
 *   - { type: 'add',     item: {...} }        → empile
 *   - { type: 'add',     item: {...}, max: N } → empile en respectant cap N (drop oldest)
 *   - { type: 'update',  id, message }        → modifie le message du toast id (no-op si absent)
 *   - { type: 'dismiss', id }                 → retire (no-op si absent)
 *   - autre                                   → renvoie items inchangé
 */
export function toastReducer(items, action) {
  switch (action && action.type) {
    case 'add': {
      const next = [...items, action.item];
      if (typeof action.max === 'number' && next.length > action.max) {
        return next.slice(next.length - action.max);
      }
      return next;
    }
    case 'update': {
      let touched = false;
      const next = items.map(t => {
        if (t.id === action.id) {
          touched = true;
          return { ...t, message: action.message };
        }
        return t;
      });
      return touched ? next : items;
    }
    case 'dismiss': {
      const next = items.filter(t => t.id !== action.id);
      return next.length === items.length ? items : next;
    }
    default:
      return items;
  }
}
```

- [ ] **Step 3 : Vérifier syntaxe (Node parse)**

```bash
node --check frontend/src/components/lf-toast-stack.logic.js
```

Expected: pas de sortie, exit 0.

- [ ] **Step 4 : Commit**

```bash
git add frontend/src/components/lf-toast-stack.logic.js
git commit -m "feat(toast): pure logic module for lf-toast-stack (reducer + dur table)"
```

---

## Task 3 : Tests unitaires du reducer (TDD)

**Files:**
- Modify: `frontend/tests/core.test.cjs` (append section)

Le projet teste en CJS en **réimplémentant la logique inline** (pattern existant pour `fmtd`, `esc`, etc.). On suit le même pattern.

- [ ] **Step 1 : Ajouter une section de test à la fin de `core.test.cjs`**

Append at end of `frontend/tests/core.test.cjs`, **avant** la ligne finale qui logge le total :

```js
// =============================================================================
// N. lf-toast-stack.logic — reducer pur
// =============================================================================
section('components/lf-toast-stack.logic.js -- toastReducer');

const TOAST_TYPES = ['info', 'success', 'error', 'warning', 'loading'];
const TOAST_DUR = { info: 3000, success: 2600, error: 8000, warning: 6000, loading: 120000 };

function normalizeType(t) {
  return TOAST_TYPES.includes(t) ? t : 'info';
}

function resolveDuration(type, explicitDur) {
  if (typeof explicitDur === 'number' && explicitDur > 0) return explicitDur;
  return TOAST_DUR[normalizeType(type)];
}

function toastReducer(items, action) {
  switch (action && action.type) {
    case 'add': {
      const next = [...items, action.item];
      if (typeof action.max === 'number' && next.length > action.max) {
        return next.slice(next.length - action.max);
      }
      return next;
    }
    case 'update': {
      let touched = false;
      const next = items.map(t => {
        if (t.id === action.id) {
          touched = true;
          return { ...t, message: action.message };
        }
        return t;
      });
      return touched ? next : items;
    }
    case 'dismiss': {
      const next = items.filter(t => t.id !== action.id);
      return next.length === items.length ? items : next;
    }
    default:
      return items;
  }
}

(function () {
  // normalizeType
  assert(normalizeType('info')     === 'info',    'normalizeType: info → info');
  assert(normalizeType('unknown')  === 'info',    'normalizeType: inconnu → info');
  assert(normalizeType(undefined)  === 'info',    'normalizeType: undefined → info');
  assert(normalizeType('error')    === 'error',   'normalizeType: error → error');

  // resolveDuration
  assert(resolveDuration('info')         === 3000,   'resolveDuration: info défaut');
  assert(resolveDuration('error')        === 8000,   'resolveDuration: error défaut');
  assert(resolveDuration('loading')      === 120000, 'resolveDuration: loading défaut');
  assert(resolveDuration('info', 5000)   === 5000,   'resolveDuration: override explicite');
  assert(resolveDuration('info', 0)      === 3000,   'resolveDuration: 0 → défaut');
  assert(resolveDuration('info', -1)     === 3000,   'resolveDuration: négatif → défaut');
  assert(resolveDuration('xxx', undefined) === 3000, 'resolveDuration: type inconnu → info default');

  // reducer add
  let s = [];
  s = toastReducer(s, { type: 'add', item: { id: 1, message: 'a', type: 'info' } });
  assert(s.length === 1 && s[0].id === 1, 'reducer add: empile 1');

  s = toastReducer(s, { type: 'add', item: { id: 2, message: 'b', type: 'info' } });
  assert(s.length === 2 && s[1].id === 2, 'reducer add: empile 2 (ordre préservé)');

  // immutabilité
  const prev = s;
  s = toastReducer(s, { type: 'add', item: { id: 3, message: 'c', type: 'info' } });
  assert(prev.length === 2, 'reducer add: ne mute pas l\'array source');
  assert(s !== prev, 'reducer add: retourne un nouvel array');

  // cap max
  s = toastReducer([{ id: 1 }, { id: 2 }, { id: 3 }, { id: 4 }, { id: 5 }],
                   { type: 'add', item: { id: 6 }, max: 5 });
  assert(s.length === 5,            'reducer add+max: respecte la cap');
  assert(s[0].id === 2,             'reducer add+max: drop le plus ancien');
  assert(s[4].id === 6,             'reducer add+max: garde le plus récent');

  // dismiss
  s = toastReducer([{ id: 1 }, { id: 2 }, { id: 3 }], { type: 'dismiss', id: 2 });
  assert(s.length === 2 && !s.find(t => t.id === 2), 'reducer dismiss: retire l\'id');

  // dismiss id absent
  const before = [{ id: 1 }, { id: 2 }];
  const after  = toastReducer(before, { type: 'dismiss', id: 999 });
  assert(after === before, 'reducer dismiss: id absent → retourne la même référence');

  // update
  s = toastReducer([{ id: 1, message: 'old' }], { type: 'update', id: 1, message: 'new' });
  assert(s[0].message === 'new',   'reducer update: message changé');
  assert(s.length === 1,           'reducer update: pile inchangée en taille');

  // update id absent
  const beforeU = [{ id: 1, message: 'x' }];
  const afterU  = toastReducer(beforeU, { type: 'update', id: 999, message: 'y' });
  assert(afterU === beforeU, 'reducer update: id absent → retourne la même référence');

  // action inconnue
  const sameRef = [{ id: 1 }];
  assert(toastReducer(sameRef, { type: 'wat' }) === sameRef, 'reducer: action inconnue → no-op identité');
  assert(toastReducer(sameRef, null) === sameRef,            'reducer: action null → no-op identité');
}());
```

- [ ] **Step 2 : Run tests, expect PASS (reducer logique trivialement correcte)**

```bash
npm test
```

Expected: ligne `OK: ... | KO: 0` à la fin. Aucune erreur. Les nouveaux asserts (`lf-toast-stack.logic`) apparaissent dans la sortie.

Si KO > 0, corriger le test ou la duplication inline avant de passer à la suite.

- [ ] **Step 3 : Commit**

```bash
git add frontend/tests/core.test.cjs
git commit -m "test(toast): unit tests for lf-toast-stack reducer logic"
```

---

## Task 4 : Web Component `<lf-toast-stack>`

**Files:**
- Create: `frontend/src/components/lf-toast-stack.js`

Le composant reproduit **l'intégralité du comportement de l'ancien `toast()`** : a11y, types, durées, cap 5, click-to-dismiss, bouton close pour `error`/`warning`, progress bar, retour d'un handle `{ remove, update }`.

- [ ] **Step 1 : Écrire le composant**

Créer `frontend/src/components/lf-toast-stack.js` :

```js
// LibreFlow — lf-toast-stack.js
// Web Component Lit : pile de notifications éphémères.
// Préserve le contrat de l'ancien ui.js#toast() / toastWithAction() :
// - 5 types (info, success, error, warning, loading)
// - Durées par type (TOAST_DUR)
// - A11Y : role=alert + aria-live=assertive pour error/warning ;
//          aria-live=polite (sur :host) sinon
// - Cap MAX_TOASTS = 5
// - Click sur le toast → dismiss
// - Bouton close × pour error/warning
// - Progress bar linéaire qui se vide
// - Méthode push(...) retourne un handle { remove, update } (= ancien remove + remove.update)

import { LitElement, html, css } from 'lit';
import { toastReducer, resolveDuration, normalizeType } from './lf-toast-stack.logic.js';

const MAX_TOASTS = 5;

const _TOAST_ICONS = {
  info:    html`<svg viewBox="0 0 10 10" fill="#fff" width="9" height="9"><circle cx="5" cy="3" r="1"/><rect x="4.2" y="4.8" width="1.6" height="3.2" rx=".8"/></svg>`,
  success: html`<svg viewBox="0 0 10 10" fill="none" stroke="#fff" stroke-width="1.6" stroke-linecap="round" width="9" height="9"><polyline points="2,5.5 4,7.5 8,3"/></svg>`,
  error:   html`<svg viewBox="0 0 10 10" fill="none" stroke="#fff" stroke-width="1.8" stroke-linecap="round" width="9" height="9"><line x1="2.5" y1="2.5" x2="7.5" y2="7.5"/><line x1="7.5" y1="2.5" x2="2.5" y2="7.5"/></svg>`,
  warning: html`<svg viewBox="0 0 10 10" fill="#fff" width="9" height="9"><path d="M5 1.5L9 8.5H1Z" fill="none" stroke="#fff" stroke-width="1.4"/><rect x="4.3" y="4" width="1.4" height="2.5" rx=".7"/><circle cx="5" cy="7.3" r=".65"/></svg>`,
  loading: html`<svg viewBox="0 0 10 10" fill="none" stroke="#fff" stroke-width="1.8" stroke-linecap="round" width="9" height="9"><path d="M5 1.5A3.5 3.5 0 1 1 1.7 3.7"/></svg>`,
};

export class LfToastStack extends LitElement {
  static properties = {
    _items: { state: true },
  };

  static styles = css`
    :host {
      position: fixed;
      bottom: 16px;
      right: 16px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      z-index: 9999;
      pointer-events: none;
      font-family: var(--lf-font-ui, system-ui, sans-serif);
    }
    .t-item {
      pointer-events: auto;
      position: relative;
      color: var(--lf-toast-fg, #fff);
      padding: 8px 12px;
      border-radius: 6px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, .3);
      display: flex;
      align-items: center;
      gap: 8px;
      min-width: 220px;
      overflow: hidden;
      animation: t-in 150ms ease-out;
      cursor: pointer;
    }
    .t-item.t-out { animation: t-out 200ms ease-in forwards; }
    .t-info    { background: var(--lf-toast-bg-info,    #222); }
    .t-success { background: var(--lf-toast-bg-success, #2a7); }
    .t-error   { background: var(--lf-toast-bg-error,   #a33); }
    .t-warning { background: var(--lf-toast-bg-warning, #c80); }
    .t-loading { background: var(--lf-toast-bg-loading, #46a); }
    .t-icon { flex: 0 0 auto; display: flex; align-items: center; }
    .t-msg  { flex: 1 1 auto; }
    .t-action {
      flex: 0 0 auto;
      background: rgba(255,255,255,.18);
      border: none;
      color: inherit;
      padding: 2px 8px;
      border-radius: 4px;
      cursor: pointer;
      font: inherit;
    }
    .t-close {
      flex: 0 0 auto;
      background: transparent;
      border: none;
      color: inherit;
      font-size: 16px;
      line-height: 1;
      cursor: pointer;
      padding: 0 4px;
    }
    .t-bar {
      position: absolute;
      left: 0; bottom: 0;
      height: 2px;
      width: 100%;
      transform-origin: left center;
      background: rgba(255,255,255,.4);
    }
    @keyframes t-in  { from { transform: translateX(20px); opacity: 0; } }
    @keyframes t-out { to   { transform: translateX(20px); opacity: 0; } }
  `;

  constructor() {
    super();
    /** @type {Array<{ id: number, message: string, type: string, duration: number,
     *                 action?: { label: string, onClick: Function },
     *                 closable: boolean }>} */
    this._items = [];
    this._timers = new Map();  // id → setTimeout handle (jamais sérialisé)
  }

  /**
   * API publique appelée par ui.js façade.
   * @param {Object} opts
   * @param {string} opts.message
   * @param {string} [opts.type='info']
   * @param {number} [opts.duration]                — override la durée par type
   * @param {{label: string, onClick: Function}} [opts.action]
   * @returns {{ remove: Function, update: Function }} handle
   */
  push(opts) {
    const type = normalizeType(opts.type);
    const id = ++LfToastStack._seq;
    const closable = (type === 'error' || type === 'warning');
    const duration = resolveDuration(type, opts.duration);

    const item = {
      id,
      message: String(opts.message ?? ''),
      type,
      duration,
      action: opts.action || null,
      closable,
    };

    this._items = toastReducer(this._items, { type: 'add', item, max: MAX_TOASTS });

    if (duration > 0 && duration < Infinity) {
      const handle = setTimeout(() => this._dismiss(id), duration);
      this._timers.set(id, handle);
    }

    return {
      remove: () => this._dismiss(id),
      update: (newMsg) => this._update(id, newMsg),
    };
  }

  _dismiss(id) {
    const t = this._timers.get(id);
    if (t) { clearTimeout(t); this._timers.delete(id); }
    this._items = toastReducer(this._items, { type: 'dismiss', id });
    this.dispatchEvent(new CustomEvent('lf-toast-dismiss', {
      detail: { id }, bubbles: true, composed: true,
    }));
  }

  _update(id, message) {
    this._items = toastReducer(this._items, { type: 'update', id, message: String(message ?? '') });
  }

  _onItemClick(id) {
    this._dismiss(id);
  }

  _onCloseClick(ev, id) {
    ev.stopPropagation();
    this._dismiss(id);
  }

  _onActionClick(ev, id, onClick) {
    ev.stopPropagation();
    try { typeof onClick === 'function' && onClick(); }
    finally { this._dismiss(id); }
    this.dispatchEvent(new CustomEvent('lf-toast-action', {
      detail: { id }, bubbles: true, composed: true,
    }));
  }

  render() {
    return html`
      ${this._items.map(t => html`
        <div
          class="t-item t-${t.type}"
          role=${t.closable ? 'alert' : 'status'}
          aria-live=${t.closable ? 'assertive' : 'polite'}
          @click=${() => this._onItemClick(t.id)}
        >
          <span class="t-icon" aria-hidden="true">${_TOAST_ICONS[t.type] ?? _TOAST_ICONS.info}</span>
          <span class="t-msg">${t.message}</span>
          ${t.action ? html`
            <button class="t-action"
                    @click=${(ev) => this._onActionClick(ev, t.id, t.action.onClick)}>
              ${t.action.label}
            </button>
          ` : null}
          ${t.closable ? html`
            <button class="t-close" aria-label="Fermer"
                    @click=${(ev) => this._onCloseClick(ev, t.id)}>×</button>
          ` : null}
          <span class="t-bar" aria-hidden="true"
                style="transition: transform ${t.duration}ms linear; transform: scaleX(0);">
          </span>
        </div>
      `)}
    `;
  }
}
LfToastStack._seq = 0;
customElements.define('lf-toast-stack', LfToastStack);
```

- [ ] **Step 2 : Vérifier syntaxe (Node parse, sans exécuter — lit n'est pas chargeable en Node sans bundler)**

```bash
node --check frontend/src/components/lf-toast-stack.js
```

Expected: pas de sortie, exit 0.

- [ ] **Step 3 : Vérifier que Vite build l'inclut sans erreur**

```bash
npm run vite:build
```

Expected: build OK. Le composant n'est pas encore importé par `ui.js`, donc Vite peut le tree-shaker. C'est attendu — il sera importé à la Task 5.

- [ ] **Step 4 : Commit**

```bash
git add frontend/src/components/lf-toast-stack.js
git commit -m "feat(toast): Lit Web Component lf-toast-stack preserving full toast() API"
```

---

## Task 5 : Façade `ui.js` — déléguer au composant sans casser l'API

**Files:**
- Modify: `frontend/src/ui.js` (remplacer `toast()` et `toastWithAction()`, garder les autres exports intacts)
- Possible modif: `frontend/src/style.css` (variables `--lf-toast-bg-*`)

### Étape A — Ajouter les variables CSS (Shadow DOM héritage)

- [ ] **Step 1 : Vérifier les couleurs actuelles de toast dans style.css**

```bash
grep -n "t-info\|t-success\|t-error\|t-warning\|t-loading" frontend/src/style.css | head -20
```

Récupérer les couleurs background utilisées par les anciennes classes `.t-info`/`.t-success`/etc. dans `style.css`. (Si elles n'existent pas, les couleurs hard-codées du composant — `#222`, `#2a7`, etc. — sont utilisées comme fallback `var(..., #...)`.)

- [ ] **Step 2 : Si les couleurs diffèrent, ajouter les CSS custom properties sur `:root`**

Si les couleurs anciennes diffèrent, append dans `frontend/src/style.css`, sous le bloc `:root { ... }` existant :

```css
:root {
  /* Lit Web Components — toast theming */
  --lf-toast-fg:         #fff;
  --lf-toast-bg-info:    /* couleur existante ou #222 */;
  --lf-toast-bg-success: /* couleur existante ou #2a7 */;
  --lf-toast-bg-error:   /* couleur existante ou #a33 */;
  --lf-toast-bg-warning: /* couleur existante ou #c80 */;
  --lf-toast-bg-loading: /* couleur existante ou #46a */;
  --lf-font-ui:          /* font-family existante */;
}
```

Si les couleurs hard-codées du composant conviennent visuellement, **aucune modif** de `style.css` n'est nécessaire — sauter cette étape.

### Étape B — Remplacer `toast()` et `toastWithAction()` par la façade

- [ ] **Step 3 : Remplacer le bloc `toast` et `toastWithAction` dans `ui.js`**

Dans `frontend/src/ui.js`, remplacer le bloc ligne ~22 (`// ── Constantes ───`) jusqu'à la fin de `toastWithAction()` (~ligne 134) par :

```js
// ── Constantes ────────────────────────────────────────────────────────────
// Note Phase 0 Lit : les constantes _TOAST_ICONS et _TOAST_DUR vivent désormais
// dans frontend/src/components/lf-toast-stack.{js,logic.js}. ui.js délègue.

import './components/lf-toast-stack.js';

let _stack = null;

/** Trouve ou crée le singleton <lf-toast-stack> attaché à document.body. */
function _getStack() {
  if (_stack && _stack.isConnected) return _stack;
  _stack = document.querySelector('lf-toast-stack');
  if (!_stack) {
    _stack = document.createElement('lf-toast-stack');
    document.body.appendChild(_stack);
  }
  return _stack;
}

// ── Toast ─────────────────────────────────────────────────────────────────

/**
 * Affiche une notification temporaire.
 * @param {string} m    Message
 * @param {string} type 'info' | 'success' | 'error' | 'warning' | 'loading'
 * @returns {Function & { update: Function }} Fonction remove() — ferme le toast manuellement.
 *          La fonction expose aussi remove.update(newMsg) pour modifier le message.
 */
export function toast(m, type = 'info') {
  const stack = _getStack();
  if (!stack) return Object.assign(() => {}, { update: () => {} });
  const handle = stack.push({ message: m, type });
  const remove = () => handle.remove();
  remove.update = (newMsg) => handle.update(newMsg);
  return remove;
}

/**
 * Toast avec bouton d'action intégré (ex : "Annuler" après suppression).
 * @param {string}   m        Message principal
 * @param {string}   type     Type
 * @param {string}   label    Label du bouton action
 * @param {Function} onAction Callback exécuté au clic
 * @param {number}   [dur]    Durée ms (défaut = durée par type)
 * @returns {Function & { update: Function }}
 */
export function toastWithAction(m, type = 'info', label, onAction, dur) {
  const stack = _getStack();
  if (!stack) return Object.assign(() => {}, { update: () => {} });
  const handle = stack.push({
    message: m,
    type,
    duration: dur,
    action: { label, onClick: onAction },
  });
  const remove = () => handle.remove();
  remove.update = (newMsg) => handle.update(newMsg);
  return remove;
}
```

**Important** :
- `esc()` (juste avant) reste exporté inchangé.
- Le focus trap `_trapFocus()` et `confirmAction()` / `promptAction()` / `resolveConfirm()` **plus bas dans le fichier** restent inchangés.
- Si l'élément `<div id="toast-shelf">` existe encore dans `index.html`, il devient orphelin mais inoffensif. Le retirer est une amélioration mineure mais **hors scope Phase 0** (le mentionner en Phase 1 si on veut nettoyer).

- [ ] **Step 4 : Sanity check syntaxe**

```bash
node --check frontend/src/ui.js
```

Expected: pas de sortie, exit 0.

- [ ] **Step 5 : `npm test` reste vert**

```bash
npm test
```

Expected: tous les asserts passent, KO: 0.

- [ ] **Step 6 : Vérifier que les call sites n'ont pas été touchés**

```bash
git diff --stat
```

Expected: seuls `ui.js` (et éventuellement `style.css`) sont modifiés. Aucun autre fichier `frontend/src/*.js` n'apparaît.

- [ ] **Step 7 : Build et smoke manuel obligatoire**

```bash
npm run vite:build
```

Expected: build OK, taille de bundle reportée par Vite. Noter la valeur.

Puis lancer l'app et vérifier visuellement :

```bash
npm run dev
```

Smoke checks à effectuer dans la fenêtre Tauri (DevTools ouvert) :
1. Une opération qui déclenche un toast `info` (par exemple : déclencher un toast via la console : `(await import('/src/ui.js')).toast('test info', 'info')`)
2. Idem `success`, `error`, `warning`, `loading`
3. Vérifier auto-dismiss aux durées attendues (info 3s, error 8s)
4. Click sur un toast → dismiss immédiat
5. Click sur × d'un `error` → dismiss immédiat
6. `error` annoncé en `assertive` (DevTools → Accessibility tree)
7. Au-delà de 5 toasts simultanés, les plus anciens disparaissent
8. `const r = toast('a'); r.update('b'); r();` → texte change, puis disparaît

Si une de ces vérifs échoue : ne PAS commit. Revenir corriger.

- [ ] **Step 8 : Commit**

```bash
git add frontend/src/ui.js
# si style.css modifié :
# git add frontend/src/style.css
git commit -m "refactor(toast): delegate ui.js#toast() to <lf-toast-stack> Web Component"
```

---

## Task 6 : Test visuel Playwright

**Files:**
- Create: `frontend/tests/visual/lit-toast.spec.js`

L'objectif est un snapshot reproductible vérifiant que le composant rend correctement. La suite Playwright existe déjà (`npm run test:visual`), donc on y ajoute un fichier.

- [ ] **Step 1 : Examiner un test visuel existant pour le pattern**

```bash
ls frontend/tests/visual/
```

Ouvrir un fichier `.spec.js` existant pour récupérer le pattern de setup (URL, ouverture page, await). Reproduire le même style.

- [ ] **Step 2 : Écrire le test**

Créer `frontend/tests/visual/lit-toast.spec.js`. Adapter `BASE_URL`, helpers, et stratégie de chargement de page au pattern observé Step 1. Exemple générique (à adapter au pattern observé) :

```js
// LibreFlow — Visual test : <lf-toast-stack>
const { test, expect } = require('@playwright/test');

test.describe('lf-toast-stack — Phase 0 Lit pilot', () => {
  test('renders an info toast (snapshot)', async ({ page }) => {
    await page.goto('/');  // adapter à la BASE_URL du config

    // Déclencher un toast info via le module ESM exposé
    await page.evaluate(async () => {
      const ui = await import('/src/ui.js');
      ui.toast('Snapshot test info', 'info');
    });

    // Attendre que le Web Component soit attaché
    await page.waitForSelector('lf-toast-stack');
    await page.waitForTimeout(200);  // animation slide-in

    // Snapshot du shadow root (Playwright supporte les shadow DOM nativement)
    const stack = page.locator('lf-toast-stack');
    await expect(stack).toHaveScreenshot('lf-toast-info.png', {
      // tolerance par défaut OK pour Phase 0
    });
  });

  test('renders an error toast with close button', async ({ page }) => {
    await page.goto('/');
    await page.evaluate(async () => {
      const ui = await import('/src/ui.js');
      ui.toast('Snapshot test error', 'error');
    });
    await page.waitForSelector('lf-toast-stack');
    await page.waitForTimeout(200);
    const stack = page.locator('lf-toast-stack');
    await expect(stack).toHaveScreenshot('lf-toast-error.png');
  });
});
```

- [ ] **Step 3 : Générer les snapshots initiaux**

```bash
npm run test:visual:update
```

Expected: la commande génère deux fichiers `.png` dans `frontend/tests/visual/lit-toast.spec.js-snapshots/` (ou équivalent selon config).

Inspecter visuellement les snapshots (ouvrir dans Explorer ou un viewer image) pour valider qu'ils ressemblent à ce qu'on attend.

- [ ] **Step 4 : Vérifier que les tests passent**

```bash
npm run test:visual
```

Expected: 2 tests verts.

- [ ] **Step 5 : Commit**

```bash
git add frontend/tests/visual/lit-toast.spec.js
git add frontend/tests/visual/lit-toast.spec.js-snapshots/  # ou chemin réel
git commit -m "test(visual): Playwright snapshots for <lf-toast-stack>"
```

---

## Task 7 : Mesure perf + ajustement budget si nécessaire

**Files:**
- Possible modif: `perf-budgets.json` (rééquilibrage du budget bundle, **uniquement si dépassé**)

- [ ] **Step 1 : Lire le budget actuel**

```bash
cat perf-budgets.json
```

Noter la valeur actuelle du budget bundle JS gz.

- [ ] **Step 2 : Lancer la chaîne perf complète**

```bash
npm run perf:check
```

Cette commande enchaîne `vite:build` + `perf:bundle` + `perf:bench` + `perf:bench-compare`. Elle exit 1 en cas de régression.

**Cas A : perf:check passe (exit 0)**

→ Bundle Lit absorbé sous le budget existant. Aucune modif de `perf-budgets.json`. Sauter Step 3.

**Cas B : perf:check échoue sur bundle (dépasse le budget)**

→ Mesurer la délta exacte. Lire le rapport, noter la nouvelle taille gz. Étape 3 obligatoire.

**Cas C : perf:check échoue sur bench (régression > 5 %)**

→ **STOP**. Ne pas modifier `perf-budgets.json`. Investiguer : la façade `_getStack()` est-elle appelée dans une boucle ? Le composant trigger-t-il un re-render abusif ? Ouvrir un sous-task de debug AVANT de continuer Phase 0.

- [ ] **Step 3 (cas B uniquement) : Rééquilibrer le budget avec justification**

Mettre à jour `perf-budgets.json` :

```json
{
  "bundle": {
    "js_gz": <nouvelle_valeur>,
    "_note": "Augmenté de +X KB en Phase 0 Lit integration (spec 2026-05-28). lit + lit-html + @lit/reactive-element + lf-toast-stack."
  }
}
```

(Adapter à la structure exacte du fichier — peut être un schéma différent.)

- [ ] **Step 4 : Re-run perf:check pour confirmer vert**

```bash
npm run perf:check
```

Expected: exit 0.

- [ ] **Step 5 : Commit (uniquement si `perf-budgets.json` a été modifié)**

```bash
git add perf-budgets.json
git commit -m "chore(perf): rebalance bundle budget for Lit integration (+X KB gz)"
```

Si aucun fichier n'a été modifié dans cette task, sauter le commit.

---

## Task 8 : Mettre à jour CLAUDE.md

**Files:**
- Modify: `CLAUDE.md` (paragraphe Lit)

- [ ] **Step 1 : Identifier où ajouter la note**

```bash
grep -n "frontend\|Stack\|architecture" CLAUDE.md | head -20
```

Localiser une section décrivant le stack frontend ou la structure du code. À défaut, ajouter une nouvelle sous-section après la dernière section pertinente.

- [ ] **Step 2 : Ajouter le paragraphe**

Insérer (à l'endroit identifié) :

```markdown
### Web Components Lit (Phase 0+)

Depuis 2026-05-28, libreflow utilise [Lit](https://lit.dev) (3.x, JS pur, sans decorators) pour les nouveaux composants UI réutilisables, hébergés dans `frontend/src/components/`. Convention :

- Préfixe `lf-` pour tous les Custom Elements (`<lf-toast-stack>`, …).
- Un fichier = un composant. Side-effect import depuis `app.js` ou via une façade (`ui.js` pour `<lf-toast-stack>`).
- Logique pure extraite dans `<nom>.logic.js` (testable depuis `core.test.cjs`).
- Shadow DOM par défaut ; thématisation via CSS custom properties (`--lf-*`) définies sur `:root`.

**Exclusions permanentes** : `virt.js`, chaîne Web Audio (`player.js`, `eq.js`, `replaygain.js`), `app.js` boot sequence, animations canvas (`cinema.js`, `viz.js`, `motion.js`, `oscPremium.js`), `ipc.js` — restent impératifs (cf. spec `docs/superpowers/specs/2026-05-28-lit-integration-design.md` §4).

Migration phasée — Phase 0 : `<lf-toast-stack>` uniquement. Phases ultérieures (modales, panneaux, sidebar) feront chacune l'objet d'une spec et d'un plan distincts.
```

- [ ] **Step 3 : Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude-md): document Lit Web Components convention (Phase 0)"
```

---

## Task 9 : Validation finale + récap

- [ ] **Step 1 : Run l'intégralité des gates locales**

```bash
npm test
npm run perf:check
npm run vite:build
```

Expected: tous verts.

- [ ] **Step 2 : Run la suite cargo (rien ne devrait avoir changé côté Rust, mais on confirme)**

```bash
cd src-tauri && cargo test && cd ..
```

Expected: vert.

- [ ] **Step 3 : Smoke manuel final**

```bash
npm run dev
```

Dans l'app, déclencher au moins 5 toasts réels via des actions utilisateur normales (ex. importer un dossier, supprimer un track, etc.) — couvrir au moins 3 types parmi {info, success, error, warning, loading}. Si tu vois `<lf-toast-stack>` dans le DOM (DevTools → Elements) et que les toasts s'affichent visuellement correctement → ✅

- [ ] **Step 4 : Récapituler les commits**

```bash
git log --oneline master..HEAD
```

Expected: 6 à 9 commits préfixés (`feat(deps)`, `feat(toast)`, `test(toast)`, `refactor(toast)`, `test(visual)`, éventuellement `chore(perf)`, `docs(claude-md)`).

- [ ] **Step 5 : Préparer le récap pour la PR**

À ce stade, Phase 0 est livrée. La création de PR sort du scope de ce plan (relève d'un step ultérieur, après revue manuelle de l'utilisateur).

Récap attendu :
- `lit@^3.x` ajouté à `dependencies`
- `<lf-toast-stack>` créé, intégrant tout le comportement de l'ancien `toast()`
- `ui.js` réduit à une façade (~40 lignes au lieu de ~110)
- Tests unitaires `toastReducer` ajoutés
- Tests visuels Playwright ajoutés
- `perf-budgets.json` ajusté si nécessaire (avec justification)
- CLAUDE.md documenté
- Tous les call sites de `toast()` / `toastWithAction()` continuent de fonctionner sans modification

---

## Auto-revue rapide (post-plan)

**Spec coverage (1:1 avec §8 critères d'acceptation de la spec) :**
- ✅ `lit` ajouté à `dependencies` → Task 1
- ✅ `lf-toast-stack.js` créé → Task 4
- ✅ `lf-toast-stack.logic.js` créé → Task 2
- ✅ `ui.js#toast()` / `toastWithAction()` deviennent façade → Task 5
- ✅ Call sites non modifiés → Task 5 step 6 (vérification `git diff --stat`)
- ✅ `perf:bundle` mesuré, budget respecté ou rééquilibré → Task 7
- ✅ `perf:bench` aucune régression > 5 % → Task 7
- ✅ `npm test` vert + tests reducer ajoutés → Tasks 3, 5, 9
- ✅ Test visuel Playwright snapshot → Task 6
- ✅ Pas de nouvel appel réseau → Task 1 step 3 + smoke Task 5 step 7
- ✅ CLAUDE.md mis à jour → Task 8
- ✅ `cargo test` vert → Task 9

**Placeholder scan :** aucun "TBD", "TODO", "à compléter" subsistant dans les steps. Les conditions "cas A/B/C" de Task 7 sont explicites, pas des placeholders.

**Type/signature consistency :**
- `toastReducer(items, action)` : signature identique entre `lf-toast-stack.logic.js` (Task 2) et le test inline (Task 3) et l'usage composant (Task 4). ✅
- `handle.remove()` / `handle.update()` : structure identique entre composant (Task 4) et façade `ui.js` (Task 5). ✅
- `push(opts)` : `opts.message` / `opts.type` / `opts.duration` / `opts.action` cohérent Task 4 ↔ Task 5. ✅
- `normalizeType` / `resolveDuration` : utilisé Task 4, défini Task 2, testé Task 3. ✅
