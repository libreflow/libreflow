// LibreFlow — waveform.js
// Waveform désactivée — player bar en mode progress bar simple (pfill).
// Les exports sont conservés pour ne pas modifier app.js.

export function initWaveform() {}
export function wfLoad()   {}
export function wfUpdate() {}
export function wfClear()  {}
