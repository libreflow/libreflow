# Review — 2026-06-12 · branche fix/audit-2026-06-11

Audit multi-agents (code-reviewer frontend, rust-reviewer) + corrections.
Baseline avant corrections de cette session : npm test 439/439, cargo test 9/9.

---

## Bugs corrigés lors de cette session

| # | Sév | Bug | Fichier(s) |
|---|-----|-----|-----------|
| 1 | HIGH | Double-escaping dans `queue.js` : `esc(t.name)` dans `itemLbl`/`rmvLbl` puis `esc(itemLbl)` à l'injection → lecteurs d'écran liraient `&amp;#39;` au lieu de `'` (régression WCAG AT) | `queue.js` lignes 357-358, 384 — esc() interne supprimé |
| 7 | MEDIUM | `setEQBand()` ne mettait pas à jour `_currentGains[idx]` après `setTargetAtTime` → `getCurrentGains()` retournait la valeur pré-drag ; `cfgsave.js` et `eqdevice.js` persistaient des gains périmés | `eq.js` ligne 352 — `_currentGains[idx] = val` ajouté après le ramp |
| 8 | MEDIUM | `saveCurrentDeviceProfile()` tombait en fallback `new Array(10).fill(0)` si `getCurrentGains()` retourne null → profil zéro-fill écrasait silencieusement un profil valide | `eqdevice.js` ligne 76 — guard + early return avec `console.warn` |
| 9 | MEDIUM | Table de percent-encoding SMTC incomplète : `'` `^` `` ` `` `{` `}` `|` absents → URI `file:///` invalide pour usernames contenant ces chars | `smtc.rs` lignes 370-375 — 6 encodages ajoutés |
| 2 | HIGH | `dupes-panel` manquait `tabindex="-1"` → `panel.focus()` no-op si `.dupes-close` absent, focus piège cassé (WCAG 2.1.2) | `index.html` — `tabindex="-1"` ajouté |
| 3 | MEDIUM | `position_secs` overflow `Duration` silencieux (fini mais énorme via IPC) → seek SMTC muet, sans log | `smtc.rs` — `unwrap_or_else` avec `log::warn!` |
| 4 | LOW | `let _ = fs::remove_file(old)` × 3 — intention non documentée | `smtc.rs` — remplacé par `drop(...)` + commentaire best-effort |
| 5 | FIX | `MediaNextTrack`/`MediaPreviousTrack` rejetés par tauri-plugin-global-shortcut (noms VK Windows vs W3C `KeyboardEvent.code`) | `main.rs` — corrigé en `MediaTrackNext`/`MediaTrackPrevious` |
| 6 | FIX | SMTC spam `HRESULT 0x800700A1` (souvlaki 0.8 UNABLE_TO_MASK_PATH sur file:// URI) | `smtc.rs` — fallback `set_metadata` sans `cover_url` si premier appel échoue |

---

## Non corrigé — décisions ouvertes (héritage audit 2026-06-11)

- **M-1** `sync_channel(8)` back-pressure : si souvlaki stalle (COM/WinRT lent), le thread
  Tauri bloque dans `send_cmd`. Acceptable pour une fréquence faible (changement de piste),
  à documenter dans le code.
- **M-2** `is_safe_dir` garde UNC (`\\`) code mort pour chemins canonicalisés. Politique réseau
  incohérente — décision d'intention requise.
- **L-1** `read_tags` timeout 8 s fuit thread `spawn_blocking` sur NAS mort. Trade-off documenté.
- **L-2** `opener:allow-reveal-item-in-dir` scopée `"**"` — pourrait être restreinte aux racines.
- **L-3** `app.js:780` accède à `__TAURI__.window` hors `ipc.js` (façade incomplète).
- **L-4** store IDB `imports` absent du CLAUDE.md §8 (doc drift).
- **L-5** Rotation pochette SMTC `% 4` : race résiduelle si Windows ne libère pas l'URI avant
  réutilisation du slot. UUID suffirait à fermer définitivement.
- **Artefacts** : `_patch_app.js` (codemod appliqué) et `src-tauri/data/` (fichier vide égaré)
  à supprimer manuellement.

---

## Verts (déclarés CLEAN par les agents)

- Tous les invariants §2 CLAUDE.md (tracks[]→rebuild, volume, IPC via ipc.js, radioRefillQueue avant updateBar, innerHTML esc())
- WCAG 2.1/2.2 AA : focus management, aria-labels, role/aria-modal corrects après corrections
- Garantie offline §15 : 0 appel réseau runtime
- Sécurité IPC Rust : validation entrées, caps, canonicalisation
- `Duration::Copy` utilisé correctement dans fallback cover smtc.rs

---

## Résultats suites après corrections

`npm test` 439/439 ✅ · `cargo test` 9/9 ✅ (après 3 MEDIUM supplémentaires fixés)

## Verdict

**WARN** → **APPROVE** — 2 HIGH + 3 MEDIUM corrigés dans cette session, 0 CRITICAL. Issues MEDIUM/LOW restantes documentées dans la section "Non corrigé" — aucune ne bloque le merge.
