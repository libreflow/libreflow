# Deep Audit — 2026-06-10 (master)

**Mode** : audit complet du codebase (pas de diff local — HEAD `41ecce9`), 9 agents reviewers parallèles + vérification manuelle de chaque finding avant patch.
**Décision** : tous les findings confirmés ont été patchés. Validation complète verte.

## Validation

| Check | Résultat |
|---|---|
| `cargo clippy -- -D warnings` | ✅ Pass (échouait avec 7 erreurs avant le patch) |
| `cargo test` | ✅ 9/9 |
| `npm test` | ✅ 398/398 |
| `vite build` | ✅ (warning chunk >500 kB préexistant) |

## Bugs patchés (18)

### CRITICAL
1. **Rust — clippy gate cassé** : 7 erreurs `-D warnings` (closures redondantes `commands.rs:620,703,706,798`, `watch.rs:89` ; casts inutiles `taskbar.rs:181-182`). Corrigé mécaniquement.

### HIGH
2. **`index.html:375` — bouton "Vider quand même" mort** : `data-action="confirm-clear"` n'existait pas dans `_ACTIONS` (handlers.js). La modale de vidage de bibliothèque ne faisait rien. → `data-action="clear-library"` (handler existant ; `clearLibrary()` ferme déjà la modale).
3. **`renderer.js:130` — roving tabindex cassé** : quand la piste courante est hors de la fenêtre virtualisée, `filteredIdx()` retournait un fi qu'aucune ligne rendue ne porte → aucune ligne `tabindex="0"`, liste inaccessible au clavier (WCAG 2.1.1). → fallback "première ligne visible" restauré.
4. **`playlist-nav.js` — section "Récentes" jamais rendue** : le bloc de rendu a été perdu lors d'un refactor alors que la déduplication `shownRecentIds` (section 3) le suppose présent → les playlists récentes hors dossier **disparaissaient de la sidebar**. → bloc restauré (présent dans le commit initial `f1ede00`). NB : le finding agent initial ("double rendu") était inversé — re-diagnostiqué à la main.
5. **`playlist-crud.js:189` — suppression d'une playlist non vide sans re-render nav** : la branche non-vide n'émettait pas `EVENTS.PLAYLIST_CHANGED` (la branche vide oui) → playlist supprimée toujours affichée. → emit ajouté.
6. **`shortcuts.js` — raccourcis single-key actifs sous le panneau raccourcis** : `#shortcuts-panel` bascule `.open` (pas `.on`) et échappe au guard `_anyModalOpen` → Space/S/R/flèches agissaient pendant la lecture du panneau. → guard `isShortcutsOpen()` (Escape et `?` exclus).
7. **`modal.js:111` — focus trap jamais armé pour le panneau raccourcis** : l'observer ne testait que `.on` alors que settings.js bascule `.open` → Tab sortait du dialog. → observer accepte `.on` **ou** `.open`.
8. **`cinema-viz.js:308` — visualiseur gelé définitivement après onglet caché** : `document.hidden` → `_cinVizRaf = null; return` tuait la boucle ; le handler `visibilitychange` de cinema.js ne relance que liquid/aurora avec `opacity==='0'` (jamais vrai ici). → replanifier pendant hidden (rAF gelé par le navigateur, reprise auto). NB : diagnostic agent corrigé — le vrai bug était l'absence de reprise, pas une fuite de handle.
9. **`watchfolder.js:145` — éviction casse-sensible du snapshot (Windows)** : un chemin différant par la casse entre événement watcher et `t.path` était évincé → réimport en doublon. → comparaison lowercase.
10. **`dupes.js:34` — faux groupes de doublons** : pistes au titre normalisé vide partageaient la clé `"|artiste"` → fausses détections proposant de supprimer des pistes valides. → skip si titre vide.
11. **`ipc.js:75` — unhandledrejection après timeout** : quand le timeout gagne la course, le rejet tardif de la branche invoke devenait orphelin. → `.catch(() => {})` sur la branche perdante (sémantique appelant inchangée).
12. **`backup.js` — pistes fantômes si l'écriture IDB échoue** : `_batchPut` avalait l'erreur puis les pistes étaient poussées en mémoire → visibles en session, perdues au redémarrage, sans aucun signal. → `_batchPut` retourne un booléen ; toast erreur si la persistance tracks échoue (design fire-and-forget conservé).

### MEDIUM
13. **`cdaudio.js — cleanupCdCache`** : listener `timeupdate` de prefetch non annulé à l'éjection → `cd_rip_track` sur lecteur éjecté à la fin de la piste suivante. → removeEventListener ajouté.
14. **`watchfolder.js — startWatchNative`** : si le 2e `listen()` échoue, le 1er listener Tauri restait enregistré à jamais. → cleanup dans le catch.
15. **`selection.js — clearSelection`** : picker `#sel-pl-picker` + listener document orphelins quand la sélection est vidée par `subscribe('tracks')`. → cleanup ajouté.
16. **`sleep.js:104`** : fallback de restauration `audio.volume` (potentiellement déjà fadé à 0) → volume verrouillé en bas. → fallback `1` (idiome `_maxVol` de `_sleepTick`).
17. **`playlog.js — setPlayLog`** : `_pendingTs` jamais purgé au remplacement du log → accumulation de ts fantômes sur la session. → `clear()` ajouté.
18. **Divers** : Rust `commands.rs — check_paths` : rejet des composants `..` avant `is_safe_dir` (sondage d'existence hors périmètre via parent non canonicalisé). / `backup.rs` : échec du cleanup tmp loggé au lieu d'être avalé. / `cfg.js`+`tlistZoom.js` : hauteurs zoom 36/60 promues en `CFG.VIRT_ROW_H_COMPACT/_COMFORTABLE` (§10). / `lf-toast-stack.js` : dispatch `lf-toast-action` déplacé dans le `finally`.

## Findings rejetés (faux positifs, vérifiés à la main)
- `shortcuts.js` Ctrl+ArrowUp dans un input → le guard typing-target `return` toutes les branches ; le combo ne touche jamais le volume.
- `taskbar_set_has_tracks` camelCase/snake_case → Tauri 2 convertit automatiquement (cohérent avec tous les commands CD/devices).
- `tagedit.js` art en innerHTML → `esc()` + blob URL, pas de vecteur.
- `i18n.js` `data-i18n-html` → chaînes build-time, pas d'injection.
- `lf-modal` focus trap shadow/light DOM → fonctionne (vérifié par l'agent lui-même).
- `replaygain.js` use-after-null `srcBuf` → ordre try/finally correct.

## Notés, non patchés (architecture / trade-offs documentés)
- `radio.js:30`, `m3u.js:19` importent `playlists.js` (violation §6) — refactor événementiel à planifier, pas un bug runtime.
- `eq.js — updateSmartEQLoudness` ignore `sleepFading` — fonction exportée **sans aucun call site** (code mort) ; la corriger imposerait un import cross-module eq←sleep (violation §6). À traiter si un appelant est câblé.
- `cinema-viz.js — _colTween` closure-local : tween GSAP de 0,75 s continue brièvement après fermeture cinéma (objet mort, impact nul).
- Badge queue sur-compte transitoirement des ids supprimés (cosmétique).
- `artLoader.js — _domBlobUrls` : trade-off documenté dans le code.
- `dropin.js:185` / `watchfolder.js` : `loadTagsBg` fire-and-forget non borné par `TAG_LOAD_CONCURRENCY` sur gros drops — choix de design existant, à surveiller.
- `_patch_app.js`, `.perf-run2.json`, `.perf-run3.json` à la racine : scripts/artefacts one-off non suivis — à supprimer ou ignorer via .gitignore.
